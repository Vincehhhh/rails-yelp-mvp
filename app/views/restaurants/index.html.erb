<div class="container">
  <h1>Restaurants#index</h1>

  <ul> <% @restaurants.each do |restaurant| %>
    <li>  <%= link_to restaurant.name, restaurant_path(restaurant) %> -
      <%= link_to "Add a review", new_restaurant_review_path(restaurant), class: "btn btn-primary"%></li>

  <% end %>
  </ul>

  <%# <button> %>
    <%= link_to "add a new restaurant", new_restaurant_path, class: "btn btn-primary" %>
  <%# </button> %>
</div>
